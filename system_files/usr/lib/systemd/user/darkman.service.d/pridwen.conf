# darkman is enabled for every user (systemctl --global). It has no business in
# GDM's initial-setup session, where it only logs errors about a missing home.
[Unit]
ConditionUser=!gnome-initial-setup
