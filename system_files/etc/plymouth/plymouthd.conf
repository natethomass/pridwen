# Pridwen boot splash. The theme lives in /usr/share/plymouth/themes/pridwen and is
# baked into the initramfs at image build (build_files/build.sh regenerates it).
[Daemon]
Theme=pridwen
ShowDelay=0
DeviceTimeout=8
